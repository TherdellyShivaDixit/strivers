class Solution {
    public boolean binarySearch(List<Integer> arr, int k) {
        int i=0;
        while(i<arr.size())
        {
            if(arr.get(i)==k)
            {
                return true;
            }
            i++;
        }
        return false;
    }
}
