class Solution {
    public static int largest(int[] arr) {
        int high=0;
        for(int i=0;i<arr.length;i++)
        {
            if(high<arr[i])
            {
                high=arr[i];
            }
        }
        return high;
    }
}
